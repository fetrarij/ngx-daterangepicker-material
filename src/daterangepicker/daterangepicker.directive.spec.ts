import { DaterangepickerDirective } from './daterangepicker.directive';

describe('DaterangepickerDirective', () => {
  it('should create an instance', () => {
    const directive = new DaterangepickerDirective();
    expect(directive).toBeTruthy();
  });
});
